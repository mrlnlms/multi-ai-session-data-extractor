---
title: "Gemini — account-2 — Data Profile"
subtitle: "Descriptive catalog of canonical parser parquets (account-2 only)"
source-title: "Gemini account-2"
jupyter: python3
---

```{python}
#| label: setup
#| code-summary: "Setup — paths, DuckDB connection, helpers"

import sys
from pathlib import Path

_this = Path.cwd()
_project_root = _this if (_this / "src").exists() else _this.parent
sys.path.insert(0, str(_project_root))

import pandas as pd
import duckdb
from itables import init_notebook_mode
init_notebook_mode(all_interactive=False)

from src.parsers.quarto_helpers import (
    fmt_pct, fmt_int, safe_int, show_df, show_md, plotly_bar,
    has_col, has_view, table_count, setup_notebook,
)

SOURCE_KEY     = "gemini"
SOURCE_TITLE   = "Gemini (account-2)"
SOURCE_COLOR   = "#1A73E8"  # Google blue (darker)
MUTED          = "#D1D5DB"
PROCESSED      = _project_root / "data" / "processed" / "Gemini"
TABLES         = ["conversations", "messages", "tool_events", "branches"]
AUX_TABLES     = []
ACCOUNT_FILTER = "2"  # account-2 only (marloonlemes)

con = duckdb.connect()
_setup = setup_notebook(con, SOURCE_KEY, PROCESSED, TABLES, AUX_TABLES, ACCOUNT_FILTER)
```

> **Gemini account-2** (`marloonlemes`). Filtered view of the
> consolidated folder `data/processed/Gemini/`. For the two-account view:
> [`gemini.qmd`](gemini.html). For account-1: [`gemini-acc-1.qmd`](gemini-acc-1.html).

{{< include _template.qmd >}}
