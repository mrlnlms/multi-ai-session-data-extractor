# 5. Auxiliary tables

Some platforms have auxiliary tables beyond the 4 canonical ones
(conversations / messages / tool_events / branches):

- **NotebookLM**: sources, notes, outputs, guide_questions, source_guides, assets, asset links
- **Qwen / Claude.ai**: project_metadata, project_docs
- **Claude Code / Codex / Gemini CLI / ChatGPT / Claude.ai / Perplexity**: current memory/context records, immutable versions, temporal evidence
- **Grok / Kimi / Qwen / Gemini / ChatGPT / Claude.ai / Perplexity**: assets and asset links (native catalogs and file structures; evidence-based placement)

This section iterates over `AUX_TABLES` configured in the notebook setup.

```{python}
#| label: aux-config

# Schema/description config per aux table — single source of truth
# Used to render standardized schema + sample.
AUX_CONFIG = {
    "sources": {
        "title": "Sources",
        "id_col": "doc_id",
        "desc": "Sources of each NotebookLM notebook (PDFs, URLs, text). Reuses `ProjectDoc` schema.",
        "fields": {
            "doc_id": "Unique source ID",
            "project_id": "Notebook that owns the source",
            "file_name": "File name / URL title",
            "content": "Text extracted from the source",
            "content_size": "Chars in content",
            "estimated_token_count": "Estimated tokens",
            "created_at": "Upload date",
        },
    },
    "notes": {
        "title": "Notes",
        "id_col": "note_id",
        "desc": "User notes in the notebook. `kind` ∈ {note, brief}.",
        "fields": {
            "note_id": "Unique ID",
            "conversation_id": "Notebook that owns it",
            "account": "Account",
            "title": "Note title (rare)",
            "content": "Note text",
            "kind": "note (manual) or brief (generated)",
            "created_at": "Creation date",
        },
    },
    "outputs": {
        "title": "Outputs",
        "id_col": "output_id",
        "desc": "Artifacts generated by the notebook. 9 types: audio, blog, video, flashcards, quiz, data table, slide deck, infographic, mind map.",
        "fields": {
            "output_id": "Unique ID",
            "conversation_id": "Notebook that generated it",
            "account": "Account",
            "output_type": "1=audio, 2=blog, 3=video, 4=flashcards, 7=table, 8=slides, 9=info, 10=mind",
            "output_type_name": "Readable type name",
            "title": "Artifact title",
            "content": "Output text/markdown (when applicable)",
            "asset_path": "Path on disk of the binary",
            "created_at": "Generation date",
        },
    },
    "guide_questions": {
        "title": "Guide Questions",
        "id_col": "question_id",
        "desc": "Questions suggested by the model in the notebook guide (3 per notebook).",
        "fields": {
            "question_id": "Unique ID",
            "conversation_id": "Notebook that owns it",
            "question_text": "Short question (label)",
            "full_prompt": "Full prompt sent to the model",
            "order": "Order (1, 2, 3)",
        },
    },
    "source_guides": {
        "title": "Source Guides",
        "id_col": "source_id",
        "desc": "Guide generated by the model PER SOURCE: summary + tags + suggested questions. Comes from RPC tr032e.",
        "fields": {
            "source_id": "Source ID",
            "conversation_id": "Source's notebook",
            "summary": "Generated summary (~800-1000 chars)",
            "tags_json": "JSON list of tags",
            "questions_json": "JSON list of suggested questions",
        },
    },
    "project_metadata": {
        "title": "Project Metadata",
        "id_col": "project_id",
        "desc": "Project metadata (name, custom instructions, doc/file counters).",
        "fields": {
            "project_id": "Unique project ID",
            "name": "Project name",
            "description": "Description (Claude.ai)",
            "custom_instruction": "Custom instruction (Qwen)",
            "prompt_template": "System prompt (Claude.ai)",
            "created_at": "Creation date",
            "updated_at": "Last update",
            "is_starred": "Pinned (Claude.ai)",
            "docs_count": "N project docs",
            "files_count": "N binary files",
        },
    },
    "project_docs": {
        "title": "Project Docs",
        "id_col": "doc_id",
        "desc": "Knowledge files for each project, with content extracted inline.",
        "fields": {
            "doc_id": "Unique doc ID",
            "project_id": "Project that owns it",
            "file_name": "File name",
            "content": "Extracted content (chars)",
            "content_size": "Chars in content",
            "estimated_token_count": "Estimated tokens",
            "created_at": "Date added",
        },
    },
    "agent_memories": {
        "title": "Agent Memory and Account Context",
        "id_col": "memory_id",
        "desc": "Current projection of derived memory documents. Consult version and temporal-evidence tables before treating historical dates as known.",
        "fields": {
            "memory_id": "Stable source identity; web IDs include account UUID and native record/surface",
            "relative_path": "CLI relative path or web raw capture locator (optional JSON pointer)",
            "current_version_id": "Current immutable content version",
            "first_seen_at": "First extractor observation, when known",
            "last_seen_at": "Most recent extractor observation",
            "source": "Source platform or CLI",
            "account_id": "Immutable web account UUID; NULL when unobserved",
            "project_path": "Decoded cwd (NULL for global Codex memories)",
            "project_key": "CLI encoded cwd or native web Project ID when scoped to a Project",
            "file_name": "Native capture or memory file name",
            "name": "Frontmatter `name` field",
            "description": "Frontmatter `description` field",
            "kind": "CLI kind, saved_memory, project_memory, memory_summary, account_instructions, project_instructions or legacy_export",
            "content": "Native text; complete normalized JSON for web summary/instructions",
            "content_size": "Bytes in content (UTF-8 encoded)",
            "created_at": "Best effective creation date; inspect basis/confidence on the version",
            "updated_at": "Best effective update date",
            "is_preserved_missing": "Absent from a complete source observation, retained in history",
        },
    },
    "agent_memory_versions": {
        "title": "Agent Memory Versions",
        "id_col": "version_id",
        "desc": "Immutable content history. Effective dates are estimates whose basis and confidence remain explicit.",
        "fields": {
            "version_id": "Memory ID plus content SHA-256",
            "memory_id": "Owning logical document",
            "relative_path": "Full source-relative path",
            "content_sha256": "SHA-256 of the exact projected UTF-8 content",
            "source_modified_at": "Native update timestamp or observed CLI source mtime",
            "source_birth_at": "Observed filesystem birth time when available",
            "first_seen_at": "First observation of this content",
            "last_seen_at": "Latest observation of this content",
            "effective_created_at": "Best selected creation estimate",
            "effective_updated_at": "Best selected update estimate",
            "created_at_basis": "Evidence type selected for creation",
            "updated_at_basis": "Evidence type selected for update",
            "created_at_confidence": "high / medium / low / unknown",
            "updated_at_confidence": "high / medium / low / unknown",
        },
    },
    "agent_memory_temporal_evidence": {
        "title": "Agent Memory Temporal Evidence",
        "id_col": "evidence_id",
        "desc": "Observed and inferred timestamp evidence. Low-confidence inference is not equivalent to an observed date.",
        "fields": {
            "evidence_id": "Deterministic evidence ID",
            "memory_id": "Logical document",
            "version_id": "Exact content version",
            "evidence_type": "Observed or inferred evidence class",
            "timestamp": "Timestamp asserted by this evidence",
            "confidence": "high / medium / low / unknown",
            "locator": "File, manifest, revision or content locator",
            "details_json": "Machine-readable evidence details",
            "is_inference": "True when the timestamp was inferred",
        },
    },
    "assets": {
        "title": "Assets",
        "id_col": "asset_id",
        "desc": "Canonical file metadata index. Relationships and placement live in asset_links; binary content remains under data/raw or data/merged.",
        "fields": {
            "asset_id": "Native asset/file ID",
            "source": "Source platform",
            "account_id": "Immutable account UUID",
            "asset_kind": "attachment / generated / project_file / output / artifact / other",
            "asset_origin": "user / assistant / platform / imported / unknown",
            "file_name": "Original/display file name",
            "mime_type": "Observed MIME type",
            "size_bytes": "Observed byte size",
            "asset_path": "Path relative to data/ when the binary exists",
            "is_model_generated": "Whether the model generated the file",
            "is_preserved_missing": "Whether the upstream catalog entry disappeared",
            "is_binary_available": "Whether asset_path resolves locally",
            "created_at": "Upstream creation timestamp",
            "metadata_json": "Allowlisted non-secret source metadata",
        },
    },
    "asset_links": {
        "title": "Asset Links",
        "id_col": "asset_link_id",
        "desc": "Evidence-backed links between assets and messages, conversations, projects, sources, outputs, or notes.",
        "fields": {
            "asset_link_id": "Deterministic relationship UUID",
            "asset_id": "Related canonical asset ID",
            "object_type": "message / conversation / project / source / output / note",
            "object_id": "Native ID of the related object",
            "conversation_id": "Conversation context when applicable",
            "message_id": "Exact message when observable",
            "project_id": "Project context when applicable",
            "role": "input / output / context / reference / unknown",
            "ordinal": "Order within the related object when observable",
            "content_block_index": "Exact inline block position when observable",
            "metadata_json": "Allowlisted source-specific relationship metadata",
        },
    },
}
```

```{python}
#| label: aux-render
#| output: asis

from IPython.display import Markdown, display

for _aux in AUX_TABLES:
    if not has_view(con, _aux):
        continue
    _conf = AUX_CONFIG.get(_aux, {"title": _aux, "id_col": "id", "desc": "", "fields": {}})
    _t = table_count(con, _aux)
    display(Markdown(f"## 5.{AUX_TABLES.index(_aux)+1} {_conf['title']}\n\n{_conf['desc']}\n\n_Rows: **{_t:,}**_"))

    # Schema + coverage
    _cols_df = con.execute(f"DESCRIBE SELECT * FROM {_aux}").df()
    _real_cols = _cols_df["column_name"].tolist()
    _types = dict(zip(_cols_df["column_name"], _cols_df["column_type"]))
    _rows_schema = []
    for _col in _real_cols:
        _desc = _conf["fields"].get(_col, "—")
        if _t > 0:
            _n = con.execute(f'SELECT COUNT("{_col}") FROM {_aux}').fetchone()[0]
            _cov_str = fmt_pct(int(_n), _t) if _n != _t else f"{_t:,} (100%)"
        else:
            _cov_str = "—"
        _rows_schema.append({
            "Field": _col,
            "Type": _types[_col],
            "Description": _desc,
            "Coverage": _cov_str,
        })
    show_df(pd.DataFrame(_rows_schema))

    # Sample (3 most recent rows or random if no created_at)
    if _t > 0:
        _has_created = "created_at" in _real_cols
        _order = "ORDER BY created_at DESC" if _has_created else ""
        _sample = con.execute(f"SELECT * FROM {_aux} {_order} LIMIT 3").df()
        # Truncate long content for display
        for _c in _sample.columns:
            if _sample[_c].dtype == "object":
                _sample[_c] = _sample[_c].astype(str).str.slice(0, 80)
        display(Markdown("**Sample (3 most recent rows):**"))
        show_df(_sample)

    # Aggregated stats when it makes sense
    if _aux == "outputs" and "output_type_name" in _real_cols:
        _stats = con.execute("""
            SELECT output_type_name, COUNT(*) AS n,
                ROUND(AVG(LENGTH(content)), 0)::INT AS avg_chars
            FROM outputs GROUP BY output_type_name ORDER BY n DESC
        """).df()
        display(Markdown("**Counts by type:**"))
        show_df(_stats)
        fig = plotly_bar(
            _stats["output_type_name"].tolist()[::-1], _stats["n"].tolist()[::-1],
            title="Outputs by type",
            color=SOURCE_COLOR, orientation="h", height=350,
        )
        fig.show()
    elif _aux == "notes" and "kind" in _real_cols:
        _stats = con.execute("""
            SELECT kind, COUNT(*) AS n,
                ROUND(AVG(LENGTH(content)), 0)::INT AS avg_chars
            FROM notes GROUP BY kind
        """).df()
        display(Markdown("**Counts by kind:**"))
        show_df(_stats)
    elif _aux in ("sources", "project_docs") and "content_size" in _real_cols:
        _stats = con.execute(f"""
            SELECT
                COUNT(*) AS n,
                SUM(content_size) AS total_chars,
                ROUND(AVG(content_size), 0)::INT AS avg_chars,
                MAX(content_size) AS max_chars
            FROM {_aux}
        """).df()
        display(Markdown(f"**Content stats ({_aux}):**"))
        show_df(_stats.assign(total_chars=_stats["total_chars"].fillna(0).astype(int)))
    elif _aux == "assets":
        _stats = con.execute("""
            SELECT asset_kind, is_binary_available, COUNT(*) AS n
            FROM assets
            GROUP BY asset_kind, is_binary_available
            ORDER BY asset_kind, is_binary_available DESC
        """).df()
        display(Markdown("**Asset kind and local-path health:**"))
        show_df(_stats)
```
