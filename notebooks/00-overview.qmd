---
title: "Overview cross-plataforma — Data Profile"
subtitle: "Visao consolidada das 10 fontes (7 web + 3 CLI) — sem filtro"
jupyter: python3
---

```{python}
#| label: setup
#| code-summary: "Setup — paths, conexao DuckDB, helpers"

import sys
from pathlib import Path

_this = Path.cwd()
_project_root = _this if (_this / "src").exists() else _this.parent
sys.path.insert(0, str(_project_root))

import pandas as pd
import duckdb
from itables import init_notebook_mode
init_notebook_mode(all_interactive=False)

from src.parsers.quarto_helpers import (
    fmt_pct, fmt_int, safe_int, show_df, show_md, plotly_bar,
    has_col, has_view, table_count, setup_unified_views,
)

# === Config do notebook ===
SUBSET_TITLE     = "Cross-plataforma (todas)"
SOURCES_FILTER   = None  # None = todas as 10 sources
UNIFIED_DIR      = _project_root / "data" / "unified"
MUTED            = "#D1D5DB"

# Paleta consistente com os qmds per-source
SOURCE_COLORS = {
    "chatgpt":     "#74AA9C",  # ChatGPT green
    "claude_ai":   "#CC785C",  # Anthropic orange
    "perplexity":  "#20808D",  # Perplexity teal
    "qwen":        "#615CED",  # Qwen roxo
    "deepseek":    "#3B82F6",  # DeepSeek blue
    "gemini":      "#4285F4",  # Google blue
    "notebooklm":  "#F4B400",  # Google yellow
    "claude_code": "#7C3AED",  # CC roxo escuro
    "codex":       "#374151",  # Codex cinza
    "gemini_cli":  "#1A56DB",  # Gemini CLI azul escuro
}

con = duckdb.connect()
_setup = setup_unified_views(con, UNIFIED_DIR, sources_filter=SOURCES_FILTER)
```

{{< include _template_overview.qmd >}}
